#!/bin/bash
set -e

# Install linux headers
xbps-install -y linux-headers gcc make pkg-config

# Grab nvidia drivers
wget https://us.download.nvidia.com/XFree86/Linux-x86_64/$1/NVIDIA-Linux-x86_64-$1.run -O nvidia.run

# Install
chmod +x nvidia.run
./nvidia.run

# Clean
rm nvidia.run
